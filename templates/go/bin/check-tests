#!/usr/bin/env bash
set -euo pipefail

# Anchor at repo root so `go test ./...` covers the whole module
# regardless of caller's cwd.
cd "$(cd "$(dirname "$0")/.." && pwd)"

echo "Running tests..."
go test ./... "$@"
echo "Tests OK"
