name: tree-sitter CI

# Reusable PR-time check workflow for tree-sitter grammar consumers.
#
# Closes part of #107. Where `tree-sitter.yml` covers the release path
# (corpus test + wasm bundle), this workflow covers every push /
# pull_request: install Node + tree-sitter CLI deps, run the standard
# `bin/check` umbrella (or scripts/test-all fallback).
#
# What's distinct
# ---------------
# - Node-based even though the output is C: tree-sitter generate and
#   tree-sitter test are npx-driven.
# - Cache gating on package-lock.json existence (setup-node@v6 with
#   cache: 'npm' hard-fails when no lockfile; many tree-sitter repos
#   use `npm install` without one).
# - `regenerate-parser` opt-in runs `npx tree-sitter generate` before
#   the umbrella check — most consumers commit the generated
#   src/parser.c so this is off by default, but consumers that gitignore
#   it can opt in.
#
# Caller shape (paste-ready):
#
#   name: CI
#   on:
#     push:
#       branches: ['**']
#     pull_request:
#   permissions:
#     contents: read
#   jobs:
#     ci:
#       uses: arthur-debert/release/.github/workflows/tree-sitter-ci.yml@v3

on:
  workflow_call:
    inputs:
      node-version:
        description: 'Node.js version.'
        type: string
        required: false
        default: '20'
      regenerate-parser:
        description: |
          Run `npx tree-sitter generate` before the umbrella check.
          Default false — most consumers commit `src/parser.c` and
          treat it as a build artifact; consumers that gitignore it
          should opt in.
        type: boolean
        required: false
        default: false
      pre-check:
        description: |
          Optional path to a script the consumer runs before
          `bin/check` (e.g. a smoke-grammars sanity step).
        type: string
        required: false
        default: ''
      check-command:
        description: |
          The standard umbrella check entry point. Defaults to
          `bin/check`; override (e.g. `scripts/test-all`) for
          consumers still mid-migration.
        type: string
        required: false
        default: 'bin/check'
      submodules:
        description: 'Pass `submodules: recursive` on checkout.'
        type: boolean
        required: false
        default: true

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: ${{ inputs.submodules && 'recursive' || '' }}

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ inputs.node-version }}
          cache: ${{ hashFiles('package-lock.json', 'npm-shrinkwrap.json') != '' && 'npm' || '' }}

      - name: Install deps
        shell: bash
        run: |
          if [ -f package-lock.json ]; then npm ci; else npm install; fi

      - name: Regenerate parser
        if: inputs.regenerate-parser
        shell: bash
        run: npx tree-sitter generate

      - name: Pre-check hook
        if: inputs.pre-check != ''
        shell: bash
        env:
          # Make `gh` usable inside the consumer's pre-check script
          # (e.g. `gh release download` for fetching pinned deps).
          GH_TOKEN: ${{ github.token }}
        run: bash "${{ inputs.pre-check }}"

      - name: Set up Python (gate linters ruff/yamllint)
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - name: Arm the gate toolset (install bin/check + lint toolset)
        # tree-sitter-ci ran the umbrella check (default `bin/check`) WITHOUT
        # installing it first — unlike rust-ci/electron-ci/tauri-ci/go-ci/
        # vscode-ext-ci/zed-extension-ci, which all arm-gate. A consumer that
        # doesn't carry a committed bin/check therefore failed with
        # `bin/check: No such file or directory` (exit 127) — caught on
        # lex-fmt/tree-sitter-lex#72. arm-gate installs bin/check and the gate
        # toolset into the `.release/` temp dir, matching the siblings.
        uses: arthur-debert/release/.github/actions/arm-gate@canary/d7962c83a6a7

      - name: Run umbrella check
        shell: bash
        run: ${{ inputs.check-command }}
