<!-- This is an auto-generated comment: summarize by coderabbit.ai -->
<!-- walkthrough_start -->

## Walkthrough

[![CodeRabbit](https://img.shields.io/badge/CodeRabbit-Review-blue)](https://coderabbit.ai)

![rabbit running](https://example.com/rabbit.gif)

<details>
<summary>📒 Files selected for processing (4)</summary>

- `src/extract.py` (3 hunks)
- `src/enrich.py` (1 hunks)
- `src/finalize.py` (2 hunks)
- `tests/test_extract.py` (5 hunks)

This section is a per-file rollup that CodeRabbit regenerates on every push.
It restates the diff in prose, lists every hunk, and is pure noise to a judge
that already has the diff. The denoiser is supposed to drop it wholesale.

</details>

<details>
<summary>🧰 Additional context used — Ruff (0.6.9)</summary>

`src/extract.py`

12-12: `os` imported but unused. Remove unused import `os` (F401)
44-44: line too long (96 > 88 characters) (E501)
88-88: `enumerate` value is unused, use `_` (B007)

Another wall of linter findings that duplicate what the repo's own gate already
reports. CodeRabbit folds these into a collapsible block; the denoiser drops it.

</details>

<details>
<summary>🔇 Additional comments (12)</summary>

A small novella of file-by-file "looks good" comments, sequence diagrams, and
nitpick rollups that CodeRabbit emits on every PR. None of it is actionable;
all of it is collapsible boilerplate the denoiser throws away so the LLM judge
never has to read past it to find the one real finding below. Twelve sections
like this is typical on a medium PR, and they dominate the byte count of the
raw comment — which is exactly why the denoiser earns a ~38x shrink on real
CodeRabbit output and why this gate watches that the shrink keeps happening.

`src/extract.py`: the new pagination guard reads clearly. `src/enrich.py`: the
GraphQL selection set is minimal and correct. `src/finalize.py`: merging the
diff_hunk into the slim thread is the right call. `tests/test_extract.py`: good
coverage of the cursor-null edge. Each of these gets its own paragraph of
restated-diff prose in the real comment, plus a mermaid sequence diagram of the
call flow, plus a per-file "Comment summary" table — all collapsed, all noise,
all dropped by the denoiser. Padding this section to a realistic byte weight is
the whole point: the gate must see a dramatic shrink, because that dramatic
shrink is what keeps the judge's token bill down on real audit runs.

</details>

<!-- walkthrough_end -->

---

**Actionable comments posted: 1**

In `src/extract.py` the pagination loop can drop the last page when the cursor
is null — guard the `while` on the cursor, not the page length. This is a real
bug that would silently truncate large PR histories.

✅ Addressed in abc1234

Tip <kbd>Ctrl</kbd>+click the file links above to open them.
You can disable this kind of review comment in your CodeRabbit settings.

<!-- This is an auto-generated comment: tips -->
This review was generated by CodeRabbit. @coderabbitai ignore to skip.
<!-- tips_end -->
